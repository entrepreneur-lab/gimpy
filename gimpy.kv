ScreenManager:
    SettingsScreen:
    ViewerScreen:

<SettingsScreen>:
    name: "settings"     

<ViewerScreen>:
    name: "viewer"

<LoadDialog>:
    title: "Load settings from .json file"
    BoxLayout:
        size: root.size
        pos: root.pos
        orientation: "vertical"
        FileChooserListView:
            id: filechooser
            path: './'
        BoxLayout:
            size_hint_y: None
            height: 30
            Button:
                text: "Load"
                on_release: root.load_file(filechooser.path, filechooser.selection)
            Button:
                text: "Cancel"
                on_release: root.dismiss()

<SaveDialog>:
    title: "Save settings as .json file"
    BoxLayout:
        size: root.size
        pos: root.pos
        orientation: "vertical"
        FileChooserListView:
            id: filechooser
            path: './'
        TextInput:
            id: dialog_savename
            size_hint_y: None
            height: 30
        BoxLayout:
            size_hint_y: None
            height: 30
            Button:
                text: "Save"
                on_release: root.save_file(filechooser.path, filechooser.selection)
            Button:
                text: "Cancel"
                on_release: root.dismiss()
